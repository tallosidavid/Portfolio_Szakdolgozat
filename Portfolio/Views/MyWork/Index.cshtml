@model IEnumerable<Portfolio.Models.Munkaim>
@{
    ViewBag.Title = "MyWorks";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

 
<br />
 
<div class="container-fluid">
     <div class="row">
        @foreach (var f in Model)
        {
            @Html.Partial("_VideoPlayer", f)
        }
     
     </div>
</div>
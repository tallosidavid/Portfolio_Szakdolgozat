@model Portfolio.Models.Feedback
@if (!Model.Engedelyezett) /////EZT ÁT KELL ÍRNI MIKOR KÉSZ A NOTIFICATION
{
    <div class="col-md-auto">
        <div class="card" style="width:auto;height:auto;margin-top:20px;">
            <div class="card-header" style="font-weight:bold;font-style:italic;background: linear-gradient(to right, #43ff6f, #22cc4a);">
                <h4 style="font-weight:bold;">@Model.Nev</h4>
            </div>
            <div class="card-body">
                <blockquote class="blockquote mb-0">
                    <p>@Model.Velemeny</p>
                    <footer class="blockquote-footer">@Model.HozzaadasDatuma.ToShortDateString() </footer>
                    @if (User.IsInRole("Admin"))
                    {
                        <span onclick="return confirm('Are you sure you want to delete this feedback?')" style="float:right;">@Html.ActionLink("Delete", "Delete", "ManageFeedbacks", new { id = Model.Id }, new { @class = "btn btn-danger" })</span>
                        <span style="float:right;margin-right:20px;">@Html.ActionLink("Allow", "Allow", "ManageFeedbacks", new { id = Model.Id }, new { @class = "btn btn-success" })</span>
                    }
                </blockquote>
            </div>
        </div>
    </div>
}












